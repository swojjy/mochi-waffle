#include <stdio.h>

int	ft_alphanumeric(char a)
{
	if (a > 47 && a < 58)
		return (1);
	if (a > 64 && a < 91)
		return (1);
	if (a > 96 && a < 123)
		return (1);
	return (0);
}

int	ft_firstletterislowcase(char *b)
{
	if (ft_alphanumeric(b))

}
char 	*ft_strcapitalize(char *str)
{
	char *copystr;

	copystr = str;

	while (*str)
	{
		if (str -= 1 
	}
}
